<Window x:Class="LibraryManagementSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagementSystem"
        mc:Ignorable="d"
        Title="Library Management System" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Window.Resources>
        <!-- Navigation Button Style -->
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,3"
                                BorderBrush="Transparent"
                                x:Name="border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3d4f63"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Active">
                                <Setter TargetName="border" Property="BorderBrush" Value="#3498db"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogoutButton" TargetType="Button" BasedOn="{StaticResource NavButton}">
            <Setter Property="Foreground" Value="#e74c3c"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2E3B4E" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" 
                          Text="Library Management System" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Foreground="White"
                          VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock x:Name="txtUserInfo" 
                              Text="Welcome, Guest" 
                              FontSize="14" 
                              Foreground="White"
                              VerticalAlignment="Center"
                              Margin="0,0,15,0"/>
                    <Button x:Name="btnLogout" 
                           Content="Logout" 
                           Width="80" 
                           Height="30"
                           Click="BtnLogout_Click"
                           Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Member Navigation Menu -->
        <Border Grid.Row="1" Background="#34495e" x:Name="memberNavBar" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnNavDashboard" Content="Dashboard" Style="{StaticResource NavButton}" Click="NavDashboard_Click"/>
                <Button x:Name="btnNavNewArrivals" Content="New Arrivals" Style="{StaticResource NavButton}" Click="NavNewArrivals_Click"/>
                <Button x:Name="btnNavSearchBooks" Content="Search Books" Style="{StaticResource NavButton}" Click="NavSearchBooks_Click"/>
            </StackPanel>
        </Border>

        <!-- Main Content Frame -->
        <Frame x:Name="MainFrame" 
              Grid.Row="2" 
              NavigationUIVisibility="Hidden"
              Background="#F5F5F5"
              Navigated="MainFrame_Navigated"/>

        <!-- Footer -->
        <Border Grid.Row="3" Background="#2E3B4E" Padding="10">
            <TextBlock Text="© 2025 Library Management System. All rights reserved." 
                      FontSize="12" 
                      Foreground="White"
                      HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
