<Page x:Class="LibraryManagementSystem.View.Pages.MemberDashboard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Member Dashboard"
      Loaded="Page_Loaded">

    <Page.Resources>
        <!-- Badge Styles -->
        <Style x:Key="BadgeActive" TargetType="Border">
            <Setter Property="Background" Value="#d4edda"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
        <Style x:Key="BadgeActiveText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#155724"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        
        <Style x:Key="BadgeOverdue" TargetType="Border">
            <Setter Property="Background" Value="#f8d7da"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
        <Style x:Key="BadgeOverdueText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#721c24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        
        <Style x:Key="BadgeReturned" TargetType="Border">
            <Setter Property="Background" Value="#d1ecf1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
        <Style x:Key="BadgeReturnedText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#0c5460"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>

        <Style x:Key="BadgeWarning" TargetType="Border">
            <Setter Property="Background" Value="#fff3cd"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
        <Style x:Key="BadgeWarningText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#856404"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        
        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980b9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#27ae60"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#229954"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#e74c3c"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#c0392b"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FilterButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#6c757d"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5a6268"/>
                </Trigger>
                <Trigger Property="Tag" Value="Active">
                    <Setter Property="Background" Value="#4CAF50"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Background="#f5f5f5">
            <Grid MaxWidth="1200" Margin="30,30,30,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Welcome Card -->
            <Border Grid.Row="0" Background="White" Padding="30" CornerRadius="10" Margin="0,0,0,30">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="Bold" Foreground="#2c3e50">
                        <Run Text="Welcome, "/>
                        <Run x:Name="txtMemberName" Text="Member"/>
                        <Run Text="!"/>
                    </TextBlock>
                    <TextBlock Margin="0,10,0,0">
                        <Run Text="Member ID: "/>
                        <Run x:Name="txtMemberId" Text="N/A" FontWeight="Bold"/>
                    </TextBlock>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Text="Status: " VerticalAlignment="Center"/>
                        <Border x:Name="statusBadge" Style="{StaticResource BadgeActive}" Margin="5,0,0,0">
                            <TextBlock x:Name="txtStatus" Text="ACTIVE" Style="{StaticResource BadgeActiveText}"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Grid Grid.Row="1" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Active Loans Card -->
                <Border Grid.Column="0" Background="White" Padding="25" CornerRadius="10" Margin="0,0,10,0" BorderBrush="#3498db" BorderThickness="0,0,0,5">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                    </Border.Effect>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock x:Name="txtActiveLoansCount" Text="0" FontSize="36" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Active Loans" FontSize="13" Foreground="#7f8c8d" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Overdue Books Card -->
                <Border Grid.Column="1" Background="White" Padding="25" CornerRadius="10" Margin="10,0,10,0" BorderBrush="#27ae60" BorderThickness="0,0,0,5">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                    </Border.Effect>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock x:Name="txtOverdueCount" Text="No" FontSize="36" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Overdue Books" FontSize="13" Foreground="#7f8c8d" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Total Borrowed Card -->
                <Border Grid.Column="2" Background="White" Padding="25" CornerRadius="10" Margin="10,0,0,0" BorderBrush="#3498db" BorderThickness="0,0,0,5">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                    </Border.Effect>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock x:Name="txtTotalBorrowed" Text="0" FontSize="36" FontWeight="Bold" Foreground="#2c3e50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Total Borrowed" FontSize="13" Foreground="#7f8c8d" Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Current Loans Section -->
            <Border Grid.Row="2" Background="White" Padding="30" CornerRadius="10" Margin="0,0,0,30">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="?? Current Loans" FontSize="20" FontWeight="SemiBold" Foreground="#2c3e50" Margin="0,0,0,20"/>
                    <Border BorderBrush="#ecf0f1" BorderThickness="0,2,0,0" Margin="0,0,0,20"/>

                    <DataGrid x:Name="dgCurrentLoans" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              Background="White"
                              RowBackground="White"
                              AlternatingRowBackground="#f8f9fa"
                              BorderThickness="0"
                              CanUserResizeRows="False"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Book Title" Binding="{Binding BookTitle}" Width="*" MinWidth="150"/>
                            <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Loan Date" Binding="{Binding LoanDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="110"/>
                            <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="110"/>
                            <DataGridTemplateColumn Header="Status" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Padding="8,4" CornerRadius="5">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="OVERDUE">
                                                            <Setter Property="Background" Value="#f8d7da"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="ACTIVE">
                                                            <Setter Property="Background" Value="#d4edda"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Status}" FontWeight="SemiBold" FontSize="11">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="OVERDUE">
                                                                <Setter Property="Foreground" Value="#721c24"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="ACTIVE">
                                                                <Setter Property="Foreground" Value="#155724"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Fine" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Fine, StringFormat='${0:F2}'}" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#6c757d"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="OVERDUE">
                                                            <Setter Property="Foreground" Value="#dc3545"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Action" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Return Book" 
                                                Style="{StaticResource SuccessButton}" 
                                                Padding="10,5"
                                                FontSize="11"
                                                Click="ReturnBook_Click"
                                                Tag="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#ecf0f1"/>
                                <Setter Property="Foreground" Value="#2c3e50"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#ecf0f1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#ecf0f1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#f8f9fa"/>
                                        <Setter Property="Foreground" Value="#2c3e50"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- My Reservations Section -->
            <Border Grid.Row="3" Background="White" Padding="30" CornerRadius="10" Margin="0,0,0,30">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="?? My Reservations" FontSize="20" FontWeight="SemiBold" Foreground="#2c3e50" Margin="0,0,0,20"/>
                    <Border BorderBrush="#ecf0f1" BorderThickness="0,2,0,0" Margin="0,0,0,20"/>

                    <DataGrid x:Name="dgReservations" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              Background="White"
                              RowBackground="White"
                              AlternatingRowBackground="#f8f9fa"
                              BorderThickness="0"
                              CanUserResizeRows="False"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Book Title" Binding="{Binding BookTitle}" Width="*" MinWidth="150"/>
                            <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Reservation Date" Binding="{Binding ReservationDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="140"/>
                            <DataGridTextColumn Header="Expiry Date" Binding="{Binding ExpiryDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="110"/>
                            <DataGridTemplateColumn Header="Status" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource BadgeWarning}">
                                            <TextBlock Text="{Binding Status}" Style="{StaticResource BadgeWarningText}"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Action" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="? Cancel" 
                                                Style="{StaticResource DangerButton}" 
                                                Padding="8,4"
                                                FontSize="11"
                                                Click="CancelReservation_Click"
                                                Tag="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#ecf0f1"/>
                                <Setter Property="Foreground" Value="#2c3e50"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#ecf0f1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#ecf0f1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#f8f9fa"/>
                                        <Setter Property="Foreground" Value="#2c3e50"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Fine Payment Section (Only visible if unpaid fines exist) -->
            <Border x:Name="borderUnpaidFines" Grid.Row="4" Background="White" Padding="30" CornerRadius="10" Margin="0,0,0,30" Visibility="Collapsed">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="?? Fine Payment" FontSize="20" FontWeight="SemiBold" Foreground="#2c3e50" Margin="0,0,0,20"/>
                    <Border BorderBrush="#ecf0f1" BorderThickness="0,2,0,0" Margin="0,0,0,20"/>

                    <DataGrid x:Name="dgUnpaidFines" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              Background="White"
                              RowBackground="White"
                              AlternatingRowBackground="#f8f9fa"
                              BorderThickness="0"
                              CanUserResizeRows="False"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Book Title" Binding="{Binding BookTitle}" Width="*" MinWidth="150"/>
                            <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Loan Date" Binding="{Binding LoanDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="100"/>
                            <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="100"/>
                            <DataGridTextColumn Header="Return Date" Binding="{Binding ReturnDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="100"/>
                            <DataGridTemplateColumn Header="Overdue Days" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding OverdueDays, StringFormat='{}{0} days'}" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="#d32f2f"
                                                   HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Status" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource BadgeReturned}">
                                            <TextBlock Text="{Binding Status}" Style="{StaticResource BadgeReturnedText}"/>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Fine" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Fine, StringFormat='${0:F2}'}" 
                                                   FontWeight="Bold" 
                                                   Foreground="#ff9800"
                                                   FontSize="13"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Action" Width="110">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="?? Pay Now" 
                                                Style="{StaticResource PrimaryButton}" 
                                                Padding="8,4"
                                                FontSize="11"
                                                Click="PayFine_Click"
                                                Tag="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#ecf0f1"/>
                                <Setter Property="Foreground" Value="#2c3e50"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#ecf0f1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#ecf0f1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#f8f9fa"/>
                                        <Setter Property="Foreground" Value="#2c3e50"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Loan History Section -->
            <Border Grid.Row="5" Background="White" Padding="30" CornerRadius="10" Margin="0,0,0,0">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="?? Loan History" FontSize="20" FontWeight="SemiBold" Foreground="#2c3e50" Margin="0,0,0,20"/>
                    <Border BorderBrush="#ecf0f1" BorderThickness="0,2,0,0" Margin="0,0,0,20"/>

                    <!-- Filter Buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button x:Name="btnLast7Days" Content="Last 7 Days" Style="{StaticResource FilterButton}" Margin="0,0,10,0" Click="FilterHistory_Click" Tag="LAST_7_DAYS"/>
                        <Button x:Name="btnLast30Days" Content="Last 30 Days" Style="{StaticResource FilterButton}" Margin="0,0,10,0" Click="FilterHistory_Click" Tag="LAST_30_DAYS"/>
                        <Button x:Name="btnLast90Days" Content="Last 90 Days" Style="{StaticResource FilterButton}" Margin="0,0,10,0" Click="FilterHistory_Click" Tag="LAST_90_DAYS"/>
                        <Button x:Name="btnAllTime" Content="All Time" Style="{StaticResource FilterButton}" Margin="0,0,20,0" Click="FilterHistory_Click" Tag="ALL_TIME"/>

                        <ComboBox x:Name="cmbStatusFilter" Width="150" Padding="8" SelectionChanged="StatusFilter_Changed">
                            <ComboBoxItem Content="All Status" IsSelected="True" Tag=""/>
                            <ComboBoxItem Content="Active" Tag="ACTIVE"/>
                            <ComboBoxItem Content="Returned" Tag="RETURNED"/>
                            <ComboBoxItem Content="Overdue" Tag="OVERDUE"/>
                        </ComboBox>
                    </StackPanel>

                    <!-- Summary Statistics -->
                    <Grid Margin="0,15,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Total Loans Card -->
                        <Border Grid.Column="0" Background="#f8f9fa" Padding="15" CornerRadius="8" Margin="0,0,7.5,0">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock x:Name="txtHistoryTotalLoans" 
                                           Text="0" 
                                           FontSize="24" 
                                           FontWeight="Bold" 
                                           Foreground="#333333" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Total Loans" 
                                           FontSize="14" 
                                           Foreground="#666666" 
                                           Margin="0,5,0,0" 
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Returned Card -->
                        <Border Grid.Column="1" Background="#f8f9fa" Padding="15" CornerRadius="8" Margin="7.5,0,7.5,0">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock x:Name="txtHistoryReturned" 
                                           Text="0" 
                                           FontSize="24" 
                                           FontWeight="Bold" 
                                           Foreground="#333333" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Returned" 
                                           FontSize="14" 
                                           Foreground="#666666" 
                                           Margin="0,5,0,0" 
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Total Fines Card -->
                        <Border Grid.Column="2" Background="#f8f9fa" Padding="15" CornerRadius="8" Margin="7.5,0,0,0">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock x:Name="txtHistoryTotalFines" 
                                           Text="$0.00" 
                                           FontSize="24" 
                                           FontWeight="Bold" 
                                           Foreground="#ff9800" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="Total Fines" 
                                           FontSize="14" 
                                           Foreground="#666666" 
                                           Margin="0,5,0,0" 
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <DataGrid x:Name="dgLoanHistory" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None"
                              Background="White"
                              RowBackground="White"
                              AlternatingRowBackground="#f8f9fa"
                              BorderThickness="0"
                              CanUserResizeRows="False"
                              SelectionMode="Single">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Book Title" Binding="{Binding BookTitle}" Width="*" MinWidth="150"/>
                            <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*" MinWidth="120"/>
                            <DataGridTextColumn Header="Loan Date" Binding="{Binding LoanDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="110"/>
                            <DataGridTextColumn Header="Return Date" Binding="{Binding ReturnDate, StringFormat='{}{0:yyyy-MM-dd}'}" Width="110"/>
                            <DataGridTemplateColumn Header="Status" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Padding="8,4" CornerRadius="5">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="OVERDUE">
                                                            <Setter Property="Background" Value="#f8d7da"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="ACTIVE">
                                                            <Setter Property="Background" Value="#d4edda"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="RETURNED">
                                                            <Setter Property="Background" Value="#d1ecf1"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Status}" FontWeight="SemiBold" FontSize="11">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="OVERDUE">
                                                                <Setter Property="Foreground" Value="#721c24"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="ACTIVE">
                                                                <Setter Property="Foreground" Value="#155724"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="RETURNED">
                                                                <Setter Property="Foreground" Value="#0c5460"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Fine" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Fine, StringFormat='${0:F2}'}" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#6c757d"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="OVERDUE">
                                                            <Setter Property="Foreground" Value="#dc3545"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#ecf0f1"/>
                                <Setter Property="Foreground" Value="#2c3e50"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#ecf0f1"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Padding" Value="12"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#ecf0f1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#f8f9fa"/>
                                        <Setter Property="Foreground" Value="#2c3e50"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
