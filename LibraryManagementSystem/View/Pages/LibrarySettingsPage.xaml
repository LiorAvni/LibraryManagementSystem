<Page x:Class="LibraryManagementSystem.View.Pages.LibrarySettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Library Settings"
      Loaded="Page_Loaded">

    <Page.Resources>
        <!-- Button Styles -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2980b9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#95a5a6"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7f8c8d"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Navigation Menu -->
        <Border Grid.Row="0" Background="#34495e" Padding="0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnNavDashboard" 
                        Content="🏠 Dashboard" 
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0,0,0,3"
                        BorderBrush="#3498db"
                        Padding="20,12"
                        FontWeight="SemiBold"
                        FontSize="14"
                        Cursor="Hand"
                        Click="NavDashboard_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                                Padding="{TemplateBinding Padding}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                BorderBrush="{TemplateBinding BorderBrush}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3d4f63"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Background="#f5f5f5">
            <StackPanel Margin="30" MaxWidth="1000">
                
                <!-- Page Title -->
                <Border Background="White" Padding="30" CornerRadius="10" Margin="0,0,0,30">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="⚙️ Library Settings" 
                                   FontSize="28" 
                                   FontWeight="Bold" 
                                   Foreground="#2c3e50"/>
                        <TextBlock Text="Configure system-wide library settings" 
                                   FontSize="14" 
                                   Foreground="#7f8c8d" 
                                   Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Settings Section -->
                <Border Background="White" Padding="30" CornerRadius="10">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                    </Border.Effect>
                    <StackPanel>
                        
                        <!-- Fine Per Day -->
                        <Border BorderBrush="#ecf0f1" BorderThickness="0,0,0,1" Padding="0,0,0,20" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="💵 Fine Per Day" 
                                               FontSize="18" 
                                               FontWeight="SemiBold" 
                                               Foreground="#2c3e50"/>
                                    <TextBlock Text="Fine amount per day for overdue books (in dollars)" 
                                               FontSize="13" 
                                               Foreground="#7f8c8d" 
                                               Margin="0,5,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBox x:Name="txtFinePerDay" 
                                             FontSize="16" 
                                             Padding="10"
                                             Width="150"
                                             HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Loan Period Days -->
                        <Border BorderBrush="#ecf0f1" BorderThickness="0,0,0,1" Padding="0,0,0,20" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="📅 Loan Period" 
                                               FontSize="18" 
                                               FontWeight="SemiBold" 
                                               Foreground="#2c3e50"/>
                                    <TextBlock Text="Default loan period in days" 
                                               FontSize="13" 
                                               Foreground="#7f8c8d" 
                                               Margin="0,5,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBox x:Name="txtLoanPeriodDays" 
                                             FontSize="16" 
                                             Padding="10"
                                             Width="150"
                                             HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Max Loans Per Member -->
                        <Border BorderBrush="#ecf0f1" BorderThickness="0,0,0,1" Padding="0,0,0,20" Margin="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="📚 Maximum Loans" 
                                               FontSize="18" 
                                               FontWeight="SemiBold" 
                                               Foreground="#2c3e50"/>
                                    <TextBlock Text="Maximum number of active loans per member" 
                                               FontSize="13" 
                                               Foreground="#7f8c8d" 
                                               Margin="0,5,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBox x:Name="txtMaxLoansPerMember" 
                                             FontSize="16" 
                                             Padding="10"
                                             Width="150"
                                             HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Max Reservations Per Member -->
                        <Border Padding="0,0,0,20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="📌 Maximum Reservations" 
                                               FontSize="18" 
                                               FontWeight="SemiBold" 
                                               Foreground="#2c3e50"/>
                                    <TextBlock Text="Maximum number of active reservations per member" 
                                               FontSize="13" 
                                               Foreground="#7f8c8d" 
                                               Margin="0,5,0,0"
                                               TextWrapping="Wrap"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBox x:Name="txtMaxReservationsPerMember" 
                                             FontSize="16" 
                                             Padding="10"
                                             Width="150"
                                             HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Action Buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                            <Button Content="💾 Save Changes" 
                                    Style="{StaticResource PrimaryButton}"
                                    Click="SaveSettings_Click"/>
                            <Button Content="🔄 Reset" 
                                    Style="{StaticResource SecondaryButton}"
                                    Click="ResetSettings_Click"/>
                        </StackPanel>

                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
